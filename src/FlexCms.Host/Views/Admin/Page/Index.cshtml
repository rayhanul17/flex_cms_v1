@model List<FlexCms.Host.Models.Admin.PageListItemViewModel>
@{
    ViewData["Title"] = "Pages";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0">Pages</h4>
    <a asp-action="Create" class="btn btn-primary btn-sm" fcms-authorize="pages.create">
        <i class="bi bi-plus-lg"></i> New Page
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead class="table-light">
                <tr>
                    <th>Title</th>
                    <th>Slug</th>
                    <th>Parent</th>
                    <th>Status</th>
                    <th>Created</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model)
                {
                    <tr data-id="@p.Id">
                        <td>@p.Title</td>
                        <td><code>/@p.Slug</code></td>
                        <td>@(p.ParentTitle ?? "—")</td>
                        <td>
                            @if (p.IsPublished)
                            {
                                <span class="badge text-bg-success">Published</span>
                            }
                            else
                            {
                                <span class="badge text-bg-secondary">Draft</span>
                            }
                        </td>
                        <td>@p.CreatedAt.ToString("yyyy-MM-dd")</td>
                        <td class="text-end">
                            <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-outline-secondary" fcms-authorize="pages.edit">Edit</a>
                            <button class="btn btn-sm btn-outline-danger btn-delete" data-id="@p.Id" fcms-authorize="pages.delete">Delete</button>
                        </td>
                    </tr>
                }
                @if (!Model.Any())
                {
                    <tr><td colspan="6" class="text-center text-muted py-4">No pages yet.</td></tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
<script>
document.querySelectorAll('.btn-delete').forEach(btn => {
    btn.addEventListener('click', async () => {
        if (!confirm('Delete this page?')) return;
        const id = btn.dataset.id;
        const res = await fetch(`/admin/pages/${id}/delete`, {
            method: 'POST',
            headers: { 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value ?? '' }
        });
        if ((await res.json()).isSuccess) location.reload();
    });
});
</script>
<form style="display:none">@Html.AntiForgeryToken()</form>
}
