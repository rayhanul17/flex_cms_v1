@model FlexCms.Host.Models.Admin.CreateEditPostViewModel
@{
    ViewData["Title"] = "New Post";
}

<h4>New Post</h4>

<form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

            <div class="mb-3">
                <label asp-for="Title" class="form-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Slug" class="form-label"></label>
                <div class="input-group">
                    <span class="input-group-text">/blog/</span>
                    <input asp-for="Slug" class="form-control" />
                </div>
                <span asp-validation-for="Slug" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Excerpt" class="form-label"></label>
                <textarea asp-for="Excerpt" class="form-control" rows="2" placeholder="Brief summary shown in post lists..."></textarea>
            </div>

            <div class="mb-3">
                <label asp-for="Content" class="form-label"></label>
                <textarea asp-for="Content" class="form-control rich-editor" rows="14"></textarea>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="CategoryId" class="form-label">Category</label>
                    <select asp-for="CategoryId" asp-items="Model.AvailableCategories" class="form-select">
                        <option value="">— Uncategorized —</option>
                    </select>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="Tags" class="form-label">Tags <small class="text-muted">(comma separated)</small></label>
                    <input asp-for="Tags" class="form-control" placeholder="dotnet, csharp, web" />
                </div>
            </div>

            <div class="row">
                <div class="col-md-8 mb-3">
                    <label asp-for="FeaturedImageUrl" class="form-label"></label>
                    <div class="input-group">
                        <input asp-for="FeaturedImageUrl" class="form-control" placeholder="Pick an image or paste a URL" />
                        <button type="button" class="btn btn-outline-secondary" data-fcms-pick-image="#FeaturedImageUrl">
                            <i class="bi bi-images"></i> Pick
                        </button>
                        <button type="button" class="btn btn-outline-secondary" data-fcms-clear-image="#FeaturedImageUrl" title="Clear">
                            <i class="bi bi-x-lg"></i>
                        </button>
                    </div>
                    <span asp-validation-for="FeaturedImageUrl" class="text-danger"></span>
                    <div class="mt-2" data-fcms-image-preview-for="#FeaturedImageUrl"
                         style="@(string.IsNullOrEmpty(Model.FeaturedImageUrl) ? "display:none" : "")">
                        <img src="@Model.FeaturedImageUrl" alt="Featured image preview"
                             style="max-height:140px; border-radius:.375rem; border:1px solid var(--bs-border-color);" />
                    </div>
                </div>
                <div class="col-md-4 mb-3 d-flex align-items-end pb-1">
                    <div class="form-check">
                        <input asp-for="IsPublished" class="form-check-input" type="checkbox" />
                        <label asp-for="IsPublished" class="form-check-label">Publish immediately</label>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="MetaTitle" class="form-label"></label>
                    <input asp-for="MetaTitle" class="form-control" />
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="MetaDescription" class="form-label"></label>
                    <input asp-for="MetaDescription" class="form-control" />
                </div>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">Create Post</button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
            </div>
</form>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    @await Html.PartialAsync("_RichTextEditor")
    @await Html.PartialAsync("_MediaPicker")
    <script>
    document.getElementById('Title').addEventListener('input', function () {
        const slug = document.getElementById('Slug');
        if (!slug.dataset.manual) {
            slug.value = this.value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
        }
    });
    document.getElementById('Slug').addEventListener('input', function () {
        this.dataset.manual = '1';
    });
    </script>
}
